{
  "name": "gauntlet",
  "version": "0.1.0",
  "description": "A Gauntlet-styled Angband: a procedurally generated roguelike with a town, stores, classes, spells and a 3/4 top-down canvas view. Built on ssinnott/game-engine (vendored in src/lib).",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "dev": "node tools/server.ts",
    "build": "node tools/build.ts",
    "sim": "node tools/sim.ts",
    "smoke": "node tools/smoke.ts",
    "sheet": "node tools/sheet.ts",
    "check": "npm run typecheck && npm run sim && npm run smoke"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.20.3",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  }
}
